import { HOST } from "./host";

export const ADD_RESERVATION_URL = `${HOST}/reservations`;
export const MODIFY_RESERVATION_URL = `${HOST}/reservations/`;
export const DELETE_RESERVATION_URL = `${HOST}/reservations/`;
